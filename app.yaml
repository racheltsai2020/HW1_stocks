runtime: python39

handlers:
  - url: /static
    static_dir: static
  
  - url: /
    script: auto

  - url: /result
    script: main.py

  - url: /stock/.*
    script: main.py

  - url: /top
    script: main.py

env_variables:
  ENVIRONMENT: 'production'